CREATE TABLE "persons" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "notes" TEXT NOT NULL,
    "profession" TEXT NOT NULL,
    "sex" TEXT NOT NULL,
    CONSTRAINT "PK_persons" PRIMARY KEY ("id")
);
